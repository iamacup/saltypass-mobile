
import React, {Fragment} from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  StatusBar,
  TextInput,
  KeyboardAvoidingView,
} from 'react-native';

import Navigator from './navigation';

const App = () => {
  return (
    <Navigator />
  );
};

export default App;
